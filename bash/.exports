export PATH="$HOME/homebrew/bin:$HOME/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:$PATH"

# Docker CLI exports
#export DOCKER_TLS_VERIFY="1"
#export DOCKER_HOST="tcp://localhost:2376"
#export DOCKER_CERT_PATH="/Users/jeffreyh/.docker/machine/machines/default"
#export DOCKER_MACHINE_NAME="default"

# Some colors
export CLICOLOR=1
export LSCOLORS=GxFxCxDxBxegedabagaced

# Python
#export WORKON_HOME=$HOME/.virtualenvs
#export VIRTUALENVWRAPPER_PYTHON=$HOME/homebrew/bin/python
#export VIRTUALENVWRAPPER_VIRTUALENV=$HOME/homebrew/bin/virtualenv
#source $HOME/homebrew/bin/virtualenvwrapper.sh

#export PYTHONPATH=$HOME/homebrew/bin/python

# Postgres
export PATH=$HOME/homebrew/opt/postgresql@9.6/bin:$PATH

# NVM
export PATH=$HOME/bin:$PATH
export PATH=$HOME/homebrew/bin:$PATH
export NVM_DIR="$HOME/.nvm"

# Load nvm
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
# Load nvm bash_completion
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# Project directory
export PROJECT_HOME=$HOME/Github

# Make vim the default editor
export EDITOR="vim"

# Start vim in insert mode when writing commit messages
export GIT_EDITOR='vim +startinsert'

# Larger bash history (allow 32³ entries; default is 500)
export HISTSIZE=32768
export HISTFILESIZE=$HISTSIZE
export HISTCONTROL=ignoredups
# Make some commands not show up in history
export HISTIGNORE="ls:cd:cd -:pwd:exit:date:* --help"

# Prefer US English and use UTF-8
export LANG="en_US"
export LC_ALL="en_US.UTF-8"

# Highlight section titles in manual pages
export LESS_TERMCAP_md="$ORANGE"

# Don’t clear the screen after quitting a manual page
export MANPAGER="less -X"

# Always enable colored `grep` output
export GREP_OPTIONS="--color=auto"
